// Risk levels used throughout the backend.
const RISK = {
  LOW: "LOW",
  MODERATE: "MODERATE",
  HIGH: "HIGH",
};

// Case statuses on the health worker dashboard.
const STATUS = {
  NEW: "new",
  VISITED: "visited",
  RESOLVED: "resolved",
};

module.exports = { RISK, STATUS };